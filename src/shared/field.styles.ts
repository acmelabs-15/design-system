// Generated by scripts/split-css.ts from the audited house sheet. Do not edit.
import { css } from "lit";
export const fieldCss = css`
  .field {
    margin-bottom: 12px;
  }
  .field:last-child {
    margin-bottom: 0;
  }
  .field label,
  .flabel,
  .form-label {
    display: block;
    font-size: 13px;
    line-height: 20px;
    font-weight: 400;
    color: var(--text-2);
    text-transform: capitalize;
    margin-bottom: 6px;
  }
  .form-label.plain {
    text-transform: none;
  }
  .form-label.solo {
    margin-bottom: 0;
  }
  .field input:not([type="checkbox"]):not([type="radio"]),
  .field select,
  .field textarea {
    width: 100%;
    height: var(--form);
    padding: 0 12px;
    border: 1px solid var(--border);
    border-radius: var(--r-sm);
    background: var(--surface);
    font-size: 14px;
    line-height: 20px;
    transition: var(--dur) var(--ease);
  }
  .field.sm input,
  .field.sm select {
    height: var(--form-sm);
  }
  .field select {
    padding-right: 36px;
  }
  .field.lg input,
  .field.lg select {
    height: var(--form-lg);
    font-size: 16px;
    line-height: 24px;
    border-radius: var(--r);
  }
  .field textarea {
    height: auto;
    padding: 10px 12px;
    resize: vertical;
    min-height: 100px;
    line-height: 20px;
  }
  .field textarea[rows] {
    min-height: 0;
  }
  .field.lg textarea {
    font-size: 16px;
    line-height: 24px;
    border-radius: var(--r);
  }
  .field input::placeholder,
  .field textarea::placeholder {
    color: var(--ds-gray-700);
  }
  .field input:hover,
  .field select:hover,
  .field textarea:hover {
    border-color: var(--border-hover);
  }
  .field input:focus,
  .field select:focus,
  .field textarea:focus {
    border-color: var(--border-active);
    box-shadow: var(--ds-shadow-border-small);
    outline: none;
  }
  .field input[aria-invalid="true"],
  .field select[aria-invalid="true"],
  .field textarea[aria-invalid="true"] {
    border-color: var(--warn-ink);
  }
  .field input[aria-invalid="true"]:focus,
  .field textarea[aria-invalid="true"]:focus {
    box-shadow: 0 0 0 3px var(--warn-weak);
  }
  .field .msg {
    display: block;
    font-size: 13px;
    line-height: 20px;
    color: var(--text-2);
    margin-top: 4px;
  }
  .field .msg.error {
    color: var(--warn-ink);
  }
  .field.lg .msg {
    font-size: 16px;
    line-height: 24px;
  }
  .field input[disabled],
  .field select[disabled],
  .field textarea[disabled] {
    color: var(--ds-gray-700);
    cursor: not-allowed;
    background: var(--comp);
  }
  .field input[readonly],
  .field textarea[readonly] {
    background: var(--surface-2);
    color: var(--text-2);
  }
  .field textarea[readonly] {
    background: var(--surface);
    color: var(--text);
  }
  .field .affix {
    display: flex;
    align-items: stretch;
  }
  .field .affix > span {
    display: inline-flex;
    align-items: center;
    padding: 0 12px;
    background: var(--surface-2);
    border: 1px solid var(--border);
    color: var(--ds-gray-700);
    font-size: 14px;
    line-height: 20px;
    white-space: nowrap;
  }
  .field .affix > span.plain {
    background: var(--surface);
  }
  .field .affix > span.icon {
    width: 38px;
    padding: 0;
    justify-content: center;
  }
  .field .affix > span .ic,
  .field .affix > span ::slotted(*) {
    color: var(--text-2);
  }
  .field .affix > span:first-child {
    border-right: 0;
  }
  .field .affix > span:last-child {
    border-left: 0;
  }
  .field .affix > input {
    border-radius: 0;
    flex: 1;
    min-width: 0;
  }
  .field .affix > .inset {
    position: relative;
    flex: 1;
    display: flex;
    min-width: 0;
  }
  .field .affix > .inset > input {
    width: 100%;
    border-radius: 0;
  }
  .field .affix > .inset > input.pad-r {
    padding-right: 38px;
  }
  .field .affix > .inset > input.pad-l {
    padding-left: 38px;
  }
  .field .affix > .inset > .in-ic {
    position: absolute;
    right: 12px;
    top: 50%;
    transform: translateY(-50%);
    display: inline-flex;
    color: var(--text-2);
    pointer-events: none;
  }
  .field .affix > .inset > .in-ic.start {
    left: 12px;
    right: auto;
  }
  .field .affix > :first-child,
  .field .affix > .inset:first-child > input {
    border-top-left-radius: var(--r-sm);
    border-bottom-left-radius: var(--r-sm);
  }
  .field .affix > :last-child,
  .field .affix > .inset:last-child > input {
    border-top-right-radius: var(--r-sm);
    border-bottom-right-radius: var(--r-sm);
  }
  .field .affix.rounded > :first-child,
  .field .affix.rounded > .inset:first-child > input {
    border-top-left-radius: 999px;
    border-bottom-left-radius: 999px;
  }
  .field .affix.rounded > :last-child,
  .field .affix.rounded > .inset:last-child > input {
    border-top-right-radius: 999px;
    border-bottom-right-radius: 999px;
  }
  .field.lg .affix > :first-child,
  .field.lg .affix > .inset:first-child > input {
    border-top-left-radius: var(--r);
    border-bottom-left-radius: var(--r);
  }
  .field.lg .affix > :last-child,
  .field.lg .affix > .inset:last-child > input {
    border-top-right-radius: var(--r);
    border-bottom-right-radius: var(--r);
  }
  .grid2 {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
  }
  .kbd.sm,
  .search .kbd,
  .menu .kbd,
  .side-find .kbd,
  .field .kbd {
    min-width: 20px;
    height: 20px;
    padding: 0 4px;
    font-size: 12px;
    line-height: 20px;
    font-weight: 500;
    color: var(--text-2);
  }
  .fieldset {
    position: relative;
    background: var(--surface);
    border-radius: var(--r-sm);
    box-shadow: var(--ds-shadow-border);
    min-width: 0;
  }
  @media (max-width: 760px) {
    .grid2 {
      grid-template-columns: 1fr;
    }
  }
  @media (pointer: coarse) {
    .field input:not([type="checkbox"]):not([type="radio"]),
    .field select {
      height: 44px;
    }
  }
`;
