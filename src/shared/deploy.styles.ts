// Generated by scripts/split-css.ts from the audited house sheet. Do not edit.
import { css } from "lit";
export const deployCss = css`
  .deploy-list {
    border: 1px solid var(--ds-gray-alpha-200);
    border-radius: 4px;
    overflow: hidden;
    background: var(--surface);
  }
  .deploy-row {
    display: grid;
    grid-template-columns: minmax(0, 2fr) 150px 130px minmax(0, 1.3fr) 140px 32px;
    gap: 16px;
    align-items: center;
    height: 48px;
    padding: 0 12px;
    border-bottom: 1px solid var(--ds-gray-alpha-200);
    font-size: 14px;
    line-height: 20px;
    min-width: 0;
  }
  .deploy-row:last-child {
    border-bottom: 0;
  }
  .deploy-row > * {
    min-width: 0;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .deploy-row .mono {
    font-family: var(--mono);
    font-size: 13px;
  }
  .deploy-row .when {
    display: flex;
    align-items: center;
    gap: 6px;
    justify-content: flex-end;
    color: var(--text-2);
  }
  .deploy-row .when .avatar {
    width: 20px;
    height: 20px;
    font-size: 8px;
  }
  .project-row {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 16px;
    border-bottom: 1px solid var(--ds-gray-alpha-200);
    min-width: 0;
  }
  .project-row:last-child {
    border-bottom: 0;
  }
  .project-row .logo {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: var(--text);
    color: var(--on-contrast);
    display: grid;
    place-items: center;
    flex: none;
    font-weight: 600;
  }
  .project-row .col {
    min-width: 0;
    flex: 1;
  }
  .project-row .name {
    font-size: 14px;
    line-height: 20px;
    font-weight: 500;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .project-row .sub {
    font-size: 14px;
    line-height: 20px;
    color: var(--text-2);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .project-row .commit {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 14px;
    line-height: 20px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .project-row .commit .ic {
    width: 14px;
    height: 14px;
    flex: none;
  }
  .project-row .end {
    display: flex;
    align-items: center;
    gap: 4px;
    flex: none;
  }
  .project-row .end .activity {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    box-shadow: var(--ds-shadow-border);
    display: grid;
    place-items: center;
    color: var(--text-2);
    background: var(--surface);
  }
  .project-row .end .activity.ok {
    box-shadow: 0 0 0 2px var(--ds-blue-500);
  }
  .bar-list .r {
    position: relative;
    display: flex;
    align-items: center;
    gap: 8px;
    height: 32px;
    padding: 0 8px;
    font-size: 14px;
    line-height: 32px;
    z-index: 0;
    min-width: 0;
  }
  .bar-list .r::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: var(--pct, 0%);
    background: var(--ds-gray-200);
    border-radius: var(--r-sm);
    z-index: -1;
  }
  .bar-list .r .n {
    margin-left: auto;
    font-weight: 600;
    font-variant-numeric: tabular-nums;
  }
  .bar-list .r .fav {
    width: 16px;
    height: 16px;
    border-radius: 3px;
    background: var(--text);
    color: var(--on-contrast);
    display: inline-grid;
    place-items: center;
    font-size: 8px;
    flex: none;
  }
  .bar-list .cols {
    display: flex;
    justify-content: space-between;
    font-size: 12px;
    line-height: 16px;
    font-weight: 500;
    text-transform: uppercase;
    color: var(--text-2);
    padding: 8px 8px 4px;
  }
`;
