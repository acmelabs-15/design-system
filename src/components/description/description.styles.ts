// Generated by the style generator from the reference spec. Do not edit; edit the mapping and regenerate.
import { css } from "lit";
export const descriptionCss = css`
  .description {
    margin: 0;
  }
  .description :where(.title) {
    margin-bottom: 0.5rem;
    min-height: 14px;
    font-size: 0.875rem;
    line-height: 14px !important;
    white-space: nowrap;
    color: var(--ds-gray-900);
    text-transform: capitalize;
  }
  .description :where(.title) :where(.info) {
    margin-left: 0.25rem;
    display: inline-block;
    vertical-align: -2px;
  }
  .description :where(.title) :where(.info) :where(.trigger) {
    display: inline-flex;
    height: fit-content;
    align-items: center;
  }
  .description:where(.ellipsis) {
    width: 100%;
  }
  .description:where(.right) {
    text-align: right;
  }
  .description :where(.content) {
    font-size: 0.875rem;
    line-height: 1rem !important;
    font-weight: 500;
    color: var(--ds-gray-1000);
  }
  .description:where(.ellipsis) > dd {
    text-overflow: ellipsis;
    white-space: nowrap;
    overflow: hidden;
  }
  .description:where(.ellipsis) > dt {
    text-overflow: ellipsis;
    white-space: nowrap;
    overflow: hidden;
  }
`;
