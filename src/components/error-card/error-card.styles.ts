// Generated by the style generator from the reference spec. Do not edit; edit the mapping and regenerate.
import { css } from "lit";
export const errorCardCss = css`
  .card {
    display: flex;
    list-style-type: none;
    flex-direction: column;
    border-radius: 0.5rem;
    border-style: solid;
    border-width: 1px;
    border-color: var(--ds-red-400);
    background-color: var(--ds-red-200) !important;
    padding: 1rem;
    color: var(--ds-red-900);
    box-shadow: none;
  }
  .card :where(.head) {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }
  .card :where(.retry) :where(.retry-label) {
    display: inline-block;
    text-overflow: ellipsis;
    white-space: nowrap;
    overflow: hidden;
    padding-inline: 0.375rem;
  }
  .card :where(.head) :where(.title) {
    text-align: center;
    font-family: var(--acme-font-sans);
    font-size: 16px;
    line-height: 24px;
    font-weight: 400;
    color: inherit;
  }
  .card :where(.retry) :where(.retry-label) :where(.retry-text) {
    font-size: 16px;
    line-height: 24px;
    font-weight: 500;
    color: var(--ds-red-900);
  }
  .card :where(.head) :where(.title) > slot::slotted(strong),
  .card :where(.head) :where(.title) > slot > strong {
    color: var(--ds-gray-1000) !important;
    font-weight: 550 !important;
  }
  .card:empty {
    display: none;
  }
`;
