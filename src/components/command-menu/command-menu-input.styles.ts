// Generated by the style generator from the reference spec. Do not edit; edit the mapping and regenerate.
import { css } from "lit";
export const commandMenuInputCss = css`
  .head {
    position: relative;
    border-bottom-style: solid;
    border-bottom-width: 1px;
    border-color: var(--ds-gray-alpha-400);
    background-color: var(--ds-background-100);
    padding: var(--padding);
    --padding: 12px;
  }
  .head :where(.crumbs) {
    position: relative;
    margin-top: 0;
    margin-right: 0;
    margin-bottom: 0.625rem;
    margin-left: 0;
  }
  .head :where(.field) :where(.esc) {
    margin-left: auto;
    display: none;
    height: 1.25rem;
    cursor: pointer;
    align-items: center;
    border-radius: 0.25rem;
    border-style: none;
    background-color: var(--ds-background-100);
    padding-inline: 0.25rem;
    padding-block: 0;
    font-size: 0.75rem;
    line-height: calc(1 / 0.75);
    box-shadow: var(--ds-shadow-border);
    outline-style: none;
    transition-property: background;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    transition-duration: 0.2s;
    animation-duration: 0.2s;
  }
  .head :where(.field) {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--padding);
    padding-inline: 0.25rem;
    padding-block: 0;
  }
  .head :where(.field) :where(.input) {
    height: 1.75rem;
    flex: 1;
    border-style: none;
    background-color: #0000;
    padding: 0;
    font-size: 1rem;
    line-height: 1.5;
    color: var(--ds-gray-1000);
    outline-style: none;
  }
  @media (forced-colors: active) {
    .head :where(.field) :where(.input) {
      outline: 2px solid #0000;
    }
    .head :where(.field) :where(.esc) {
      outline: 2px solid #0000;
    }
  }
  @media not all and (min-width: 401px) {
    .head {
      position: sticky;
      top: 0;
    }
  }
  @media (min-width: 401px) {
    .head :where(.field) :where(.input) {
      font-size: 1.125rem;
      line-height: calc(1.75 / 1.125);
    }
  }
  @media (hover: hover) and (pointer: fine) {
    .head :where(.field) :where(.esc) {
      display: flex;
    }
  }
  .head :where(.field) :where(.input)::placeholder {
    color: var(--ds-gray-700);
  }
  @media not all and (min-width: 401px) {
    .head :where(.crumbs) > :where(acme-breadcrumbs)::part(list) {
      margin-inline: calc(var(--padding) * -1);
    }
  }
  .head :where(.loading):after {
    content: "";
    position: absolute;
    bottom: -1.5px;
    height: 0.125rem;
    width: 50%;
    animation: cmdkLoading 1.1s cubic-bezier(0.455, 0.03, 0.515, 0.955) infinite;
    background-image: linear-gradient(to right in oklab, transparent 0%, var(--ds-gray-1000) 50%, transparent 100%);
    opacity: 1;
  }
  .head :where(.field) :where(.esc)[data-focus] {
    box-shadow: var(--ds-focus-ring);
  }
  @media not all and (min-width: 401px) {
    .head :where(.crumbs):before {
      content: "";
      position: absolute;
      top: 0;
      bottom: 0;
      left: -0.75rem;
      width: var(--padding);
      background-image: linear-gradient(to right in oklab, var(--ds-background-100) 10%, transparent 100%);
    }
    .head :where(.crumbs):after {
      content: "";
      position: absolute;
      top: 0;
      right: -0.75rem;
      bottom: 0;
      width: var(--padding);
      background-image: linear-gradient(to left in oklab, var(--ds-background-100) 10%, transparent 100%);
    }
  }
  @media (hover: hover) {
    @media (hover: hover) and (pointer: fine) {
      .head :where(.field) :where(.esc)[data-hover] {
        background-color: var(--ds-gray-100);
      }
    }
  }
  @media not all and (min-width: 401px) {
    .head :where(.crumbs) > :where(acme-breadcrumbs)::part(list) :where(acme-breadcrumb):first-of-type::part(item) {
      margin-left: var(--padding);
    }
  }
  @keyframes cmdkLoading {
    0% {
      opacity: 1;
      transform: translate(-100%);
    }
    to {
      opacity: 1;
      transform: translate(300%);
    }
  }
`;
