// Generated by the style generator from the reference spec. Do not edit; edit the mapping and regenerate.
import { css } from "lit";
export const codeBlockSwitcherCss = css`
  .switcher :where(select) {
    position: absolute;
    inset: 0;
    cursor: pointer;
    background-color: var(--ds-gray-100);
    opacity: 0;
    outline-style: none;
  }
  .switcher {
    position: relative;
    height: 2rem;
  }
  .switcher :where(.face) {
    display: flex;
    height: 100%;
    cursor: pointer;
    align-items: center;
    gap: 0.25rem;
    border-radius: 0.375rem;
    border-style: none;
    background-color: var(--ds-background-200);
    padding-inline: 0.5rem;
    padding-block: 0;
    font-family: var(--acme-font-sans);
    font-size: 13px;
    line-height: 1;
    font-weight: 400;
    white-space: nowrap;
    color: var(--ds-gray-900);
    transition-property: color, background-color, border-color, outline-color, text-decoration-color, fill, stroke, --acme-gradient-from, --acme-gradient-via, --acme-gradient-to;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    transition-duration: 0.2s;
    animation-duration: 0.2s;
  }
  .switcher :where(select) :where(option) {
    color: var(--ds-gray-1000);
  }
  @media (forced-colors: active) {
    .switcher :where(select) {
      outline-offset: 2px;
      outline: 2px solid #0000;
    }
  }
  .switcher :where(.face) > strong {
    font-weight: 500;
    color: var(--ds-gray-1000);
  }
  .switcher[data-focus-within] :where(.face) {
    box-shadow: var(--ds-focus-ring);
  }
  @media (hover: hover) {
    .switcher[data-hover] :where(.face) {
      background-color: var(--ds-gray-200);
    }
  }
`;
