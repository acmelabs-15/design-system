// Generated by the style generator from the reference spec. Do not edit; edit the mapping and regenerate.
import { css } from "lit";
export const folderCss = css`
  .folder :where(.toggle) :where(.icon) {
    margin-right: 0.5rem;
    display: inline-block;
    text-align: center;
    color: var(--ds-gray-900);
  }
  .folder :where(.toggle) {
    margin-left: -0.5rem;
    display: flex;
    height: 1.75rem;
    width: 100%;
    flex-grow: 1;
    align-items: center;
    border-radius: 0.25rem;
    border-style: none;
    background-color: #0000;
    padding-inline: 0.5rem;
    padding-block: 0.25rem;
  }
  .folder :where(.toggle) :where(.name) {
    display: block;
    overflow: hidden;
    font-family: var(--font-mono);
    text-overflow: ellipsis;
  }
  .folder {
    display: flex;
    list-style-type: none;
    flex-direction: column;
    line-height: 1.75rem;
    white-space: nowrap;
    color: var(--accents-8);
    -webkit-user-select: none;
    user-select: none;
  }
  .folder slot::slotted(a),
  .folder slot > a {
    display: block !important;
    padding-right: 1em !important;
    line-height: 1 !important;
    white-space: nowrap !important;
    color: var(--accents-8) !important;
    text-decoration-line: none !important;
    -webkit-user-select: none !important;
    user-select: none !important;
  }
  .folder :where(.toggle) :where(.name) slot::slotted(a),
  .folder :where(.toggle) :where(.name) slot > a {
    vertical-align: text-bottom !important;
    transition-property: opacity !important;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1) !important;
    transition-duration: 0.1s !important;
    animation-duration: 0.1s !important;
    animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1) !important;
  }
  .folder:where(:not(.open)) button {
    cursor: pointer;
  }
  .folder :where(.toggle) :where(.icon) svg {
    vertical-align: middle;
  }
  .folder ul {
    margin: 0;
    list-style-type: none;
    padding: 0;
  }
  @media (hover: hover) {
    .folder :where(.toggle)[data-hover] {
      background-color: var(--ds-gray-200);
    }
  }
  .folder .indent {
    display: inline-block;
    height: 1.75rem;
    width: 23px;
    flex-shrink: 0;
    translate: -0.25rem 0;
    background-image: linear-gradient(to right, transparent 11.5px, var(--indent-color) 11.5px, var(--indent-color) 12.5px, transparent 12.5px);
    background-repeat: no-repeat;
    vertical-align: top;
    --indent-color: var(--ds-gray-400);
  }
`;
