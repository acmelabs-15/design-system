// Generated by scripts/split-css.ts from the audited house sheet. Do not edit.
import { css } from "lit";
export const trendCss = css`
  .trend {
    display: inline-flex;
    align-items: center;
    gap: 3px;
    font-family: var(--mono);
    font-size: var(--t-xs);
    line-height: 16px;
    font-weight: 500;
    color: var(--text-2);
    white-space: nowrap;
    font-variant-numeric: tabular-nums;
  }
  .trend .ic {
    width: 12px;
    height: 12px;
  }
  .trend.up {
    color: var(--success-ink);
  }
  .trend.down {
    color: var(--warn-ink);
  }
  .trend.pill {
    height: 22px;
    padding: 0 8px;
    border-radius: 999px;
    background: var(--comp);
  }
  .trend.pill.up {
    background: var(--success-weak);
  }
  .trend.pill.down {
    background: var(--warn-weak);
  }
  .trend.lg {
    font-size: var(--t-sm);
  }
  .trend-note {
    font-size: var(--t-xs);
    line-height: 16px;
    color: var(--text-2);
    font-family: var(--sans);
  }
`;
