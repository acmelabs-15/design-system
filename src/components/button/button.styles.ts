// Generated by scripts/split-css.ts from the audited house sheet. Do not edit.
import { css } from "lit";
export const buttonCss = css`
.btn{display:inline-flex;align-items:center;justify-content:center;gap:6px;height:var(--form);padding:0 16px;
     background:var(--surface);color:var(--text);border:1px solid var(--border);border-radius:var(--r-sm);
     font-size:14px;line-height:20px;font-weight:500;white-space:nowrap;
     transition:border-color var(--dur) var(--ease),background var(--dur) var(--ease),color var(--dur) var(--ease),opacity var(--dur) var(--ease)}
.btn:hover{background:var(--comp);border-color:var(--border-hover)}
.btn:active{background:var(--comp-hover);border-color:var(--border-active)}
.btn.primary{background:var(--contrast-strong);color:var(--on-contrast);border-color:var(--contrast-strong)}
.btn.primary:hover{background:var(--contrast-strong-hover);border-color:var(--contrast-strong-hover)}
.btn.primary:active{background:var(--contrast-hover);border-color:var(--contrast-hover)}
.btn.tertiary,.btn.ghost{background:transparent;border-color:transparent;color:var(--text-2)}
.btn.tertiary:hover,.btn.ghost:hover{background:var(--comp);color:var(--text)}
.btn.error,.btn.danger.solid{background:var(--warn-solid);border-color:var(--warn-solid);color:var(--on-warn)}
.btn.error:hover,.btn.danger.solid:hover{background:var(--warn-solid-hover);border-color:var(--warn-solid-hover)}
.btn.danger{color:var(--warn);background:var(--surface)}
.btn.danger:hover{border-color:var(--warn);background:var(--warn-weak);color:var(--warn-ink)}
.btn.warning{background:var(--caution-solid);border-color:var(--caution-solid);color:var(--ds-gray-1000)}
.btn.sm{height:var(--form-sm);padding:0 12px;font-size:14px}
.btn.lg{height:var(--form-lg);padding:0 20px;font-size:16px;line-height:24px;border-radius:var(--r)}
.btn.icon{padding:0;width:var(--form)}
.btn.icon.sm{width:var(--form-sm)}
.btn.icon.lg{width:var(--form-lg)}
.btn.shadow{border:0;box-shadow:var(--ds-shadow-border-inset)}
.btn.rounded{border-radius:999px}
.btn.select{gap:8px;padding-right:10px;font-weight:400}
.btn.select::after{content:"";width:8px;height:8px;border-right:1.5px solid var(--text-2);border-bottom:1.5px solid var(--text-2);transform:rotate(45deg) translate(-1px,-2px);flex:none}
.btn .lead{color:var(--text-2)}
.btn-group{display:inline-flex}
.btn-group > .btn{border-radius:0}
.btn-group > .btn:first-child{border-radius:var(--r-sm) 0 0 var(--r-sm)}
.btn-group > .btn:last-child{border-radius:0 var(--r-sm) var(--r-sm) 0}
.btn-group > .btn + .btn{margin-left:-1px}
.btn-group > .btn.primary + .btn.primary{border-left-color:var(--border)}
.btn-group.split > .btn:last-child{padding:0 8px}
.btn-group > .btn.lead-cell{padding:0 10px;color:var(--text-2);background:var(--surface-2)}
.btn.block{width:100%}
.btn[disabled],.btn[aria-disabled="true"]{background:var(--comp);color:var(--ds-gray-700);border-color:var(--border);cursor:not-allowed}
.btn.loading,.btn.primary.loading,.btn.error.loading,.btn.warning.loading{background:var(--comp);color:var(--ds-gray-700);border-color:var(--border);pointer-events:none}
.btn.loading::before{content:"";width:16px;height:16px;flex:none;
     background:conic-gradient(from 0deg,color-mix(in srgb,var(--ds-gray-700) 20%,transparent),var(--ds-gray-700));
     -webkit-mask:radial-gradient(circle,transparent 39%,#000 40%),repeating-conic-gradient(from -8deg,#000 0 16deg,transparent 16deg 30deg);-webkit-mask-composite:source-in;
     mask:radial-gradient(circle,transparent 39%,#000 40%),repeating-conic-gradient(from -8deg,#000 0 16deg,transparent 16deg 30deg);mask-composite:intersect;
     animation:spin 1s steps(12) infinite}
.iconbtn{border:1px solid var(--border);background:var(--surface);border-radius:var(--r-sm);width:var(--form);height:var(--form);
         color:var(--text-2);display:inline-grid;place-items:center;transition:var(--dur) var(--ease)}
.iconbtn:hover{background:var(--comp);border-color:var(--border-hover);color:var(--text)}
.iconbtn:active{background:var(--comp-hover)}
.iconbtn.sm{width:var(--form-sm);height:var(--form-sm)}
.iconbtn.circle{border-radius:999px}
.iconbtn.tiny{width:24px;height:24px;border-radius:4px}
.iconbtn.lg{width:var(--form-lg);height:var(--form-lg);border-radius:var(--r)}
.iconbtn[disabled]{color:var(--ds-gray-700);cursor:not-allowed}
.iconbtn{position:relative}
.iconbtn.quiet{border-color:transparent;background:transparent}
.iconbtn .count{position:absolute;top:-5px;right:-5px;min-width:16px;height:16px;padding:0 4px;border-radius:999px;background:var(--accent);color:#fff;
                font-family:var(--mono);font-size:10px;line-height:12px;font-weight:600;display:grid;place-items:center;border:2px solid var(--bg)}
.btn.block.loading{justify-content:center}
@media (pointer:coarse){
  .btn,.btn.sm{height:44px;padding:0 16px}
  .btn.icon,.btn.icon.sm{width:44px;padding:0}
  .iconbtn,.iconbtn.sm{width:44px;height:44px}
}
`;
