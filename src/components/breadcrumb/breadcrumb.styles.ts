// Generated by the style generator from the reference spec. Do not edit; edit the mapping and regenerate.
import { css } from "lit";
export const breadcrumbCss = css`
  .item:where(:not(.menu)) {
    display: flex;
  }
  .item :where(.chip) {
    display: inline-block;
  }
  .item:where(.menu) {
    display: inline-flex;
    height: fit-content;
  }
  .item :where(.chip) {
    max-width: 100%;
    min-width: 0;
  }
  .item:where(:not(.menu).disabled) {
    cursor: not-allowed;
  }
  .item:where(:not(.menu):not(.disabled)) {
    cursor: pointer;
  }
  .item :where(.chip) {
    cursor: pointer;
  }
  .item {
    align-items: center;
  }
  .item:where(:not(.menu)) {
    gap: 0.375rem;
  }
  .item :where(.chip) {
    text-overflow: ellipsis;
    overflow: hidden;
    border-radius: 0.25rem;
    border-style: solid;
    border-width: 1px;
  }
  .item:where(.active) :where(.chip) {
    border-color: var(--ds-gray-600);
  }
  .item:where(:not(.active)) :where(.chip) {
    border-color: var(--ds-gray-alpha-400);
  }
  .item:where(.active) :where(.chip) {
    background-color: var(--ds-background-100);
  }
  .item:where(:not(.active):not(.disabled)) :where(.chip) {
    background-color: var(--ds-background-200);
  }
  .item:where(.disabled) :where(.chip) {
    background-color: var(--ds-gray-alpha-200);
  }
  .item :where(.chip) {
    padding-inline: 0.375rem;
    padding-block: 0.125rem;
  }
  .item:where(:not(.menu)) {
    font-size: 0.875rem;
    line-height: calc(1.25 / 0.875);
  }
  .item :where(.chip) {
    font-size: 0.75rem;
    line-height: calc(1 / 0.75);
    font-weight: var(--font-sans);
    word-break: normal;
    overflow-wrap: normal;
    white-space: nowrap;
  }
  .item:where(:not(.menu).disabled) {
    color: var(--ds-gray-700);
  }
  .item:where(.disabled) :where(.chip) {
    color: var(--ds-gray-700);
  }
  .item:where(:not(.menu):not(.active):not(.disabled)) {
    color: var(--ds-gray-900);
  }
  .item:where(:not(.active):not(.disabled)) :where(.chip) {
    color: var(--ds-gray-900);
  }
  .item:where(:not(.menu).active) {
    color: var(--ds-gray-1000);
  }
  .item:where(.active) :where(.chip) {
    color: var(--ds-gray-1000);
  }
  .item :where(.chip) {
    transition-property: color, border-color, background;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  }
  .item:where(:not(.menu)) {
    transition-property: color, background-color, border-color, outline-color, text-decoration-color, fill, stroke, --acme-gradient-from, --acme-gradient-via, --acme-gradient-to;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    transition-duration: 0.2s;
  }
  .item :where(.chip) {
    transition-duration: 0.2s;
  }
  .item:where(:not(.menu)) {
    animation-duration: 0.2s;
  }
  .item :where(.chip) {
    animation-duration: 0.2s;
  }
  @media (min-width: 401px) {
    .item:where(.menu) {
      display: inline-block;
      max-width: 100%;
      min-width: 0;
      text-overflow: ellipsis;
      white-space: nowrap;
      overflow: hidden;
      word-break: normal;
      overflow-wrap: normal;
    }
    :host([menu]) {
      max-width: 100%;
      min-width: 0;
    }
  }
  .item:where(:not(.menu)) a {
    border-radius: 0.125rem;
    color: inherit;
    text-decoration-line: none;
    box-shadow: var(--ds-focus-ring);
  }
  .item:where(:not(.menu)) svg {
    color: var(--ds-gray-900) !important;
  }
  @media (hover: hover) {
    .item :where(.chip)[data-hover] {
      border-color: var(--ds-gray-alpha-500);
      background-color: var(--ds-gray-200);
    }
    .item:where(:not(.menu))[data-hover] {
      color: var(--ds-gray-1000);
    }
    .item :where(.chip)[data-hover] {
      color: var(--ds-gray-1000);
    }
  }
  .item :where(.chip)[data-focus] {
    box-shadow: var(--ds-focus-ring);
    outline-style: none;
  }
  .item:where(.disabled) :where(.chip):disabled {
    cursor: not-allowed;
  }
  .item:where(:not(.menu)) a[data-focus] {
    outline-style: none;
  }
  :host(:last-of-type) :where(.item):where(:not(.menu)) svg {
    display: none;
  }
`;
