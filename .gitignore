node_modules
dist
*.log
.DS_Store
tools/geist/corpus/

# Census measurement output: a run's raw readings, regenerated by re-running the census.
# The configs beside them ARE tracked, because they are what makes a run repeatable.
tools/geist/census/*.geist.json
tools/geist/census/*.ours.json
